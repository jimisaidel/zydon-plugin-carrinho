export type Control = "on" | "off";

export type View = "app" | "config";

export enum AppType {
  "global" = "global",
  "checkout" = "checkout",
}

export type Layout = "horizontal" | "mini" | "full";
